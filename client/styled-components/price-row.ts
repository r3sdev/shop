import styled from 'styled-components';

export const PriceRow = styled.div`
  background: white;
  display: flex;
  align-items: flex-start;
  position: absolute;
  right: 4rem;
  bottom: 4rem;
  padding: 0.5rem;
`;
