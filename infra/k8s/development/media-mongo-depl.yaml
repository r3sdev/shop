apiVersion: apps/v1
kind: Deployment
metadata:
  name: media-mongo-depl
  namespace: development
spec:
  replicas: 1
  selector:
    matchLabels:
      app: media-mongo
  template:
    metadata:
      labels:
        app: media-mongo
    spec:
      containers:
        - name: media-mongo
          image: mongo
          env:
            - name: MONGO_DATA_DIR
              value: /data/db
            - name: MONGO_LOG_DIR
              value: /dev/null
          volumeMounts:
            - name: media-mongo-storage
              mountPath: /data/db
      terminationGracePeriodSeconds: 10
      volumes:
        - name: media-mongo-storage
          persistentVolumeClaim:
            claimName: media-mongo-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: media-mongo-srv
  namespace: development
spec:
  selector:
    app: media-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
